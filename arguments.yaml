description: Note To Self (nts)
arguments:
  - name: config
    required: False
    type: string
    multiple: False
    default: "~/.config/nts/config.toml"
    flags:
      - c
      - config
    help: "set path to config file"
